name: 01-first workflow
on: 
  pull_request:
  workflow_dispatch:
    inputs:
      branch: 
        description: 'Branch to test'
        type: string
        default: 'dev'
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello world"
