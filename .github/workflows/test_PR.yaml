name: Test PR on dev

on:
  workflow_dispatch: 
  pull_request:
    branches:
      - dev
jobs:
  j_1:
    runs-on: ubuntu-latest

    steps:

      - name: Print
        run: |
          echo "dev branch"
